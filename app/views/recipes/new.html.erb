<h1>Recipe Form Page</h1>

<%= form_for @recipe do |f| %>
    Recipe Name:
    <%= f.text_field :name %>

    User:
    <%= f.collection_select :user_id, User.all, :id, :name%>

    <%= f.submit %>
    <%end%>
